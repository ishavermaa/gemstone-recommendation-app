{
  "name": "gemstone-recommendation-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --ignore-scripts && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
